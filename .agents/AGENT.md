# AGENT

## 通用规则

- 代码符合Flake8规范。每次修改万代码都要运行`flake8`检查问题并修正（禁止修改flake8规则）。
- 每次完成编码工作，都需要主动检查相应的修改部分是否存在对应的单元测试（如果没有，添加），运行他们；
- 每次完成编码工作，都要运行功能测试代码（如果没有，添加它们）。

## 用户脚本

你要在根目录保留两个可用的py脚本:
- 实现多轮交互功能：`interactive.py`；
- 实现单次询问功能：`question.py`。
并确保它们一直是好用的。
